# LLVM IR Walkthrough

LLVM IR generation is intentionally stubbed in the starter scaffold. Once
the parser and semantic analyzer are implemented, this document should
track the IR emitted for each example program and explain the basic blocks,
allocas, loads, stores, calls, and branches.
